# Optional Stockfish location

If Stockfish is not installed on the computer, download the official macOS executable, rename it `stockfish`, and place it in this folder.

Make it executable once with:

```bash
chmod +x bin/stockfish
```

`main.py` will find it automatically. Do not commit the large engine binary to GitHub.

